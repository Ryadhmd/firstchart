apiVersion: v1
kind: Service
metadata:
  name: {{.Chart.Name}}-svc
spec:
  selector:
    app.kubernetes.io/name: {{.Chart.Name}}
  ports:
    - protocol: TCP
      port: {{.Values.containerPort}}
      targetPort: {{.Values.containerPort}}